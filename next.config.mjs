/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            
            'miro.medium.com',
            'images.chesscomfiles.com',
        ]
    }
};

export default nextConfig;
